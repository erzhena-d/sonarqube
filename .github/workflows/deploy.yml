name: sonarqube-deployment

on:
  workflow_dispatch:
    inputs:
      region:
        description: "AWS region"
        required: true
        type: choice
        options:
          - us-east-1
          - us-east-2
          - us-west-1
          - us-west-2
      action:
        description: "Terraform action"
        required: true
        type: choice
        options:
          - apply
          - destroy

jobs:
  terraform:
    runs-on: ubuntu-latest
    environment: ${{ inputs.region }}

    steps:
      - name: Git Clone
        uses: actions/checkout@v4

      - name: Run ls to verify directory structure
        run: ls -la

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3  
      
      - name: Terraform init
        run: terraform -chdir=terraform/dev init
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4.3.1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ inputs.region }}
  
      # - name: Write tfvars
      #   run: echo "${{ secrets.TF_VARS }}" > terraform/dev/terraform.tfvars
      # - name: Terraform init
      #   run: terraform -chdir=terraform/dev init
      # - name: Terraform ${{ inputs.action }}
      #   run: terraform -chdir=terraform/dev ${{ inputs.action }} -auto-approve